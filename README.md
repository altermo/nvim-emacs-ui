# Neovim UI inside Emacs

## Usage
Use `nvim` to start a neovim instance in a new buffer.

### Keymaps
Use `C-1 C-2`(`<C-1><C-2>`) to unfocus neovim (e.g. stop forwarding all inputs to neovim), and `C-1`(`<C-1>`) to refocus neovim (e.g. start forwarding all inputs to neovim).
